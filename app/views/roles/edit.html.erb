<% if agent_logged_in? %>
    <%= render'layouts/agent_header' %>
  <% end %>
<div class="role-edit-div">
	<h1>Editing Role</h1>

	<%= render 'form', role: @role %>

	<%= link_to 'Back', roles_path %>

</div>
